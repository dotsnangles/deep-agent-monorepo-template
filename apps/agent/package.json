{
  "name": "agent",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uv run python main.py",
    "start": "uv run python main.py",
    "lint": "uv run ruff check .",
    "format": "uv run ruff format ."
  }
}
